services:
  genggam-makna-api:
    image: "vinssdroid/genggam-makna-backend-api:v1"
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genggam-makna-backend-api
    env_file:
      - /opt/secrets/genggam-makna-backend-api.env
    ports:
      - "8001:8001"
    networks:
      - nginx
    volumes:
      - /opt/secrets:/opt/secrets
    command:
      ["./genggam-makna-backend-api"]

networks:
  nginx:
    external: true